"""DEPRECATED (sd_112): This module used to provide a Django Ninja router.

We no longer use Django Ninja.

The official Siddes backend API layer is Django REST Framework (DRF).

Kept as a stub so older overlays/imports don't explode when the repo is scanned.
"""

from __future__ import annotations

# Intentionally empty.
# See: siddes_inbox/urls.py + siddes_inbox/views.py
