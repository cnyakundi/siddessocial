"""Siddes Django project package.

Project name: `siddes_backend`.

We keep backend feature modules framework-agnostic (e.g. `backend/siddes_inbox/`)
so logic can be used by Django Ninja, DRF, or tests without rewriting.
"""
