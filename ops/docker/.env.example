# Siddes docker-compose dev env example
#
# Usage:
#   cd ops/docker
#   cp .env.example .env
#   docker compose -f docker-compose.dev.yml up --build
#
# NOTE: docker-compose.dev.yml already has safe defaults. This file is here so
# contributors have a clear starting point.

# --- Postgres ---
POSTGRES_DB=siddes
POSTGRES_USER=siddes
POSTGRES_PASSWORD=siddes_dev_password
POSTGRES_PORT=5432

# --- Ports (host) ---
SIDDES_BACKEND_PORT=8000
SIDDES_FRONTEND_PORT=3000

# --- Backend ---
DJANGO_DEBUG=1
DATABASE_URL=postgres://siddes:siddes_dev_password@db:5432/siddes
REDIS_URL=redis://redis:6379/0

# --- Frontend ---
# What the browser calls (host machine)
NEXT_PUBLIC_API_BASE=http://localhost:8000
# What Next.js server routes call (inside docker network)
SD_INTERNAL_API_BASE=http://backend:8000
