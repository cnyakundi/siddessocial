# Siddes Docker env example
# Copy to ops/docker/.env for local overrides (do not commit secrets).
#
# Ports (host -> container)
SIDDES_FRONTEND_PORT=3000
SIDDES_BACKEND_PORT=8000

# Browser-facing API base (host)
NEXT_PUBLIC_API_BASE=http://localhost:8000

# Container-internal API base (Next server route handlers)
SD_INTERNAL_API_BASE=http://backend:8000

# Inbox store mode: memory|db|auto (recommended: auto)
SD_INBOX_STORE=auto
SD_INBOX_DUALWRITE_DB=0

# Posts store mode: memory|db|auto (recommended: auto)
SD_POST_STORE=auto

# Public trust gates (dev)
NEXT_PUBLIC_SD_PUBLIC_TRUST_GATES=1
